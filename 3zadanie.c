#include <stdio.h>
int i,j,x;
const int n=2;
const int m=2;
int main(void) {
	
int matrix_A[n][m] = {
	{0,1},
	{0,0}
};
int matrix_B[n][m];

(A + i*m+j)=x; 
k = i*M + j;
{for (long i = k; i < n; ++i)
{
  A[i] = A[i + 1];
}
--n;}
	for (int i=0; i<n; ++i) {
        for (int j=0; j<n; ++j) {
            matrix_A[i][j] = matrix_B[j][i];
        }
	}
for (int i=0; i<n; ++i) {
        for (int j=0; j<n; ++j) {
            printf("%3i", matrix_A[i][j]);
        }
}

	return 0;
}